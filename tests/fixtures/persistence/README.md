# Published persistence fixtures

These synthetic, non-secret documents retain the v0.2.27 serialized field names,
ordering and schema semantics. They are frozen inputs, not generated by the
current writer. Keep them when adding a new schema and add another version's
fixtures rather than replacing these. Tests must prove that source bytes remain
unchanged by reads, immutable backup hashes remain valid, and migration/rollback
constraints are not weakened.
